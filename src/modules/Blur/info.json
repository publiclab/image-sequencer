{
    "name": "Blur",
    "description": "Gaussian blur an image by a given value",
    "inputs": {
        "blur": {
            "type": "integer",
            "desc": "Amount of gaussian blur(Less blur gives more detail, typically 0-5)",
            "default": 2
        }
    }
}
